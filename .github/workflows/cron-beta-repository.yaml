name: Check & build kernels in cron
on:
  schedule:
    - cron:  '*/15 * * * *' 
jobs:
  build: 
    runs-on: [runner2]
    steps:
      - name: Run script
        shell: bash {0}
        run: |
          run-one ./beta-repository.sh
