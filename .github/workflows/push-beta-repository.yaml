name: Check & build kernels at push
on:
  push:
    branches:
      - master
jobs:
  build: 
    runs-on: [runner2]
    steps:
      - name: Run script
        shell: bash {0}
        run: |
          run-one bash beta-repository.sh
