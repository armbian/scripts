name: Build kernels on push, cron but only if there are changes

on:
  push:
    branches:
      - master
  schedule:
    - cron:  '0 */15 * * *'

jobs:

  build: 
    runs-on: [runner2]
    steps:
      - name: Run script
        run: |
          pwd
